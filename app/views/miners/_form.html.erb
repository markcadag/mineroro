<%= form_for(@miner) do |f| %>
  <% if @miner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@miner.errors.count, "error") %> prohibited this mine from being saved:</h2>

      <ul>
      <% @miner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :position %><br>
    <%= f.text_field :position %>
  </div>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :mobile_number %><br>
    <%= f.text_field :mobile_number %>
  </div>

  <div class="field">
    <%= f.label :tunnel %><br>
    <%= f.collection_select :tunnel_id, @tunnels ,:id, :name, {prompt: true}, {:id => 'tunnels', style: 'margin-bottom:15px;'} %>
  </div><br>

  <div class="field">
     <%= f.label :date_hired %><br/>
      <input id='invoice_date' type="text" data-provide='datepicker' name="date_hired">
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<script type="text/javascript">

   $(document).ready(function() { $("#tunnels").select2(); });

</script>