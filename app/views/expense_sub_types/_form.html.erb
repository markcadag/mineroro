<%= form_for(@expense_sub_type) do |f| %>
  <% if @expense_sub_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense_sub_type.errors.count, "error") %> prohibited this expense_sub_type from being saved:</h2>

      <ul>
      <% @expense_sub_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col-md-6 center-block ">
        <div class="panel panel-default" style="margin-top: 20px; margin-left: 20px;">
          <div class="panel-heading"> New Expense Sub Type</div>
            <div class="panel-body">
              <form class="form-horizontal" action="" method="post">
                <fieldset>
            <div class="form-group">
              <div class="field">
                <%= f.label :expense_type_id ,:class => 'col-md-3 control-label'  %>
                <div class="col-md-9">
                   <%= f.collection_select :expense_type_id, ExpenseType.all,:id, :name, {prompt: true} ,{:id => 'expense_type', class: 'form-control', style: 'margin-bottom:15px;'} %>
                </div>
              </div>
            </div>
             <div class="form-group">
              <div class="field">
                <%= f.label :name ,:class => 'col-md-3 control-label'  %>
                <div class="col-md-9">
                  <%= f.text_field :name, style: 'margin-bottom:15px;', :class => 'form-control' %>
                </div>
              </div>
            </div>
                  <div class="form-group">
                      <div class="col-md-12 widget-right">
                          <%= f.submit 'Submit', :class => 'btn btn-primary btn-md pull-right' %>
                      </div>
                  </div>

           </fieldset>
           </form>
        </div>
      </div>
    </div>


<% end %>
